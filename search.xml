<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>hexo个人博客搭建</title>
    <url>/2026/01/15/hexo%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/</url>
    <content><![CDATA[<h2 id="引言"><a href="#引言" class="headerlink" title="引言"></a>引言</h2><p>这是一个搭建个人博客的教程，也是我的学习笔记。希望帮助每个人能按照这篇文章的步骤从零开始搭建出一个个人博客。当然，我现在只是一个学习者，正在不断摸索的阶段，如有问题还请斧正。</p>
<h3 id="关于博客的认识"><a href="#关于博客的认识" class="headerlink" title="关于博客的认识"></a>关于博客的认识</h3><p>现在我所搭建的是一个静态博客。它完全免费，不需要服务器，域名可有可无。<br>现代静态博客从技术实现架构看，有四个核心部分组成：博客生成框架（如Hexo,Hugo等）、文件托管平台（GitHub,gitee等）、站点部署服务（GitHub Pages,GitHub Actions,GitLab CI&#x2F;CD,Vercel&#x2F;Netlify等）、CDN加速服务（Cloudflare CDN、Vercel&#x2F;Netlify等）。<br>这篇博客所使用的技术实现架构：Hexo + GitHub + GitHub Pages。这里站点部署服务之所以选择GitHub Pages而不是GitHub Actions，是因为我的博客是在本地电脑构建出前端文件(HTML&#x2F;CSS&#x2F;JS文件)上传至GitHub的，GitHub Actions的脚本需要运行在源码上；至于为什么没有CDN加速服务，是我还不知道怎么配置加速，后面我会将这个加上的。</p>
<span id="more"></span>

<h3 id="写博客的好处"><a href="#写博客的好处" class="headerlink" title="写博客的好处"></a>写博客的好处</h3><p>首先，博客是可以作为一个知识输出的端口，正所谓“学然后知不足，教然后知困”，写博客就是对知识的重新认识和回顾，加深对知识的理解；其次，博客输出是一个搭建个人数字资产，一个知识库，可以作为自己日后的重要材料；最后，博客可以帮助正在学习相关知识的人，结交志同道合的伙伴。</p>
<h3 id="使用的工具及系统环境"><a href="#使用的工具及系统环境" class="headerlink" title="使用的工具及系统环境"></a>使用的工具及系统环境</h3><p>这个教程的博客结构：hexo + github + github pages，没有使用CDN加速用户的访问，主要现在我还没有去学。使用hexo生成博客目录，再上传到GitHub托管代码，然后使用GitHub的免费静态网页托管服务-GitHub Pages将网站部署。</p>
<p>我个人的系统环境是Windows11，需要使用git命令。</p>
<p>git官网：<a href="https://git-scm.com/">https://git-scm.com/</a></p>
<p>安装流程默认即可。</p>
<h2 id="Hexo博客框架"><a href="#Hexo博客框架" class="headerlink" title="Hexo博客框架"></a>Hexo博客框架</h2><h3 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h3><p>hexo是一个基于nodejs的静态博客网站生成器，作者是来自台湾的Tommy Chen，它能作为多名博主的博客框架主要有以下优点：</p>
<ul>
<li>支持MarKdown语法，编辑简单，排版优美</li>
<li>能快速生成和渲染上百页面</li>
<li>部署简单，接口简单</li>
<li>兼容于各大主流操作系统</li>
<li>社区主题和插件丰富，解决问题的参考材料多</li>
</ul>
<h3 id="环境配置"><a href="#环境配置" class="headerlink" title="环境配置"></a>环境配置</h3><p>hexo是基于nodejs的，需要它的相关环境，可以从官网直接下载</p>
<p><a href="https://nodejs.org/zh-cn/">https://nodejs.org/zh-cn/</a></p>
<p>下载后如没有特殊配置，全程默认即可。最后查看版本信息来确认环境配置信息：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">node -v # 查看node版本信息</span><br><span class="line">npm -v # 查看npm版本信息</span><br></pre></td></tr></table></figure>

<p>能够正常看到版本信息，说明环境配置已经完成。如果没有看到，提示”无法将什么识别为~”，可能是你的系统变量出现问题，可以通过手动配置系统变量解决。这里为了节省篇幅，使用简洁的方式打开系统变量：</p>
<ol>
<li><p>在Windows搜索框输入“环境变量”，快速访问到相关页面进行相关设置</p>
</li>
<li><p>点击环境变量，无论是用户变量还是系统变量重点看它们的Path，查看里面是否有“nodejs”和“npm”的词样</p>
</li>
<li><p>没有，选择任意一个变量进入Path新建变量</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">C:\Program Files\nodejs\ # 这是nodejs安装的默认路径</span><br><span class="line">C:\Users\dong\AppData\Roaming\npm # 没这个，hexo命令会无法识别</span><br></pre></td></tr></table></figure></li>
</ol>
<p>还有一点需要注意，npm默认的官网源执行时加载慢，或者执行命令时报错。可以通过以下方式将源设置为国内的淘宝镜像源。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看原npm源地址</span></span><br><span class="line">npm config get registry </span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">或使用下面的命令查看</span></span><br><span class="line">npm config list | grep registry</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">修改npm的源为淘宝源</span></span><br><span class="line">npm config set registry https://registry.npmmirror.com </span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">如果你已修改，想改回原来的源</span></span><br><span class="line">npm config set registry https://registry.npmjs.org/</span><br></pre></td></tr></table></figure>

<p>这里提一下npm淘宝镜像源的一些信息：</p>
<p>原来的地址<a href="https://registry.npm.taobao.org自2022年5月31日起停止服务,新地址为https//registry.npmmirror.com">https://registry.npm.taobao.org自2022年5月31日起停止服务，新地址为https://registry.npmmirror.com</a> 。这个得注意下，有些文档没有更新。</p>
<h3 id="生成博客"><a href="#生成博客" class="headerlink" title="生成博客"></a>生成博客</h3><h4 id="安装hexo"><a href="#安装hexo" class="headerlink" title="安装hexo"></a>安装hexo</h4><p>用命令行调用npm包管理器软件，安装hexo</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install hexo-cli -g # 全局安装hexo命令行工具</span><br></pre></td></tr></table></figure>

<p>参数解析：-g参数表示全局安装，没有这个参数就旨在当下目录安装，建议全局安装</p>
<h4 id="初始化"><a href="#初始化" class="headerlink" title="初始化"></a>初始化</h4><p>需要注意的是，想好你的博客放在哪里，然后右击后点击终端（win11）打开命令行。win10系统右击后git bash（前面安装了git会有）</p>
<p>运行下面命令</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">hexo init &quot;你的博客目录名称&quot; # 创建你的博客目录</span><br></pre></td></tr></table></figure>

<p>得到如下的反馈信息：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">INFO  Cloning hexo-starter https://github.com/hexojs/hexo-starter.git</span><br><span class="line">INFO  Install dependencies</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">一些可能的中间信息</span></span><br><span class="line">INFO  Start blogging with Hexo!</span><br></pre></td></tr></table></figure>

<p>生成了一个博客目录，里面放有一些hexo的文件。</p>
<p>进入你的博客目录：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">cd &quot;博客目录&quot;   </span><br></pre></td></tr></table></figure>

<p>安装博客需要的其他支持：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install # 安装的依赖项在package.json的文件的dependencies字段中可以看到</span><br></pre></td></tr></table></figure>

<p>这里提醒一下，后面未标注命令在哪执行，默认都在这个目录执行。</p>
<h4 id="博客项目目录结构介绍"><a href="#博客项目目录结构介绍" class="headerlink" title="博客项目目录结构介绍"></a>博客项目目录结构介绍</h4><p>本地操作展示结束，现在简单认识博客目录下文件。</p>
<p>目录结构如下：</p>
<p><img src="/2026/01/15/hexo%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/blog001.jpg" alt="images001"></p>
<p>各部分的含义：</p>
<ul>
<li>_config.yml<ul>
<li>为全局配置文件，网站的很多信息都在这里配置，比如说网站名称，副标题，描述，作者，语言，主题等等。具体可以参考官方文档：<a href="https://hexo.io/zh-cn/docs/configuration.html%E3%80%82">https://hexo.io/zh-cn/docs/configuration.html。</a></li>
</ul>
</li>
<li>scaffolds<ul>
<li>骨架文件，是生成新页面或者新博客的模版。可以根据需求编辑，当<code>hexo</code>生成新博客的时候，会用这里面的模版进行初始化。</li>
</ul>
</li>
<li>source<ul>
<li>这个文件夹下面存放的是网站的<code>markdown</code>源文件，里面有一个<code>_post</code>文件夹，所有的<code>.md</code>博客文件都会存放在这个文件夹下。现在，你应该能看到里面有一个<code>hello-world.md</code>文件。</li>
</ul>
</li>
<li>themes<ul>
<li>网站主题目录，<code>hexo</code>有非常丰富的主题支持，主题目录会存放在这个目录下面。</li>
<li>我们后续会以默认主题来演示，更多的主题参见：<a href="https://hexo.io/themes/">https://hexo.io/themes/</a></li>
</ul>
</li>
</ul>
<h4 id="本地预览博客"><a href="#本地预览博客" class="headerlink" title="本地预览博客"></a>本地预览博客</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">hexo server # 本地运行server服务预览，打开http://localhost:4000就可以预览了</span><br><span class="line">Ctrl +c  # 结束预览或者结束服务</span><br></pre></td></tr></table></figure>

<p>本地预览效果如下：</p>
<p><img src="/2026/01/15/hexo%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/blog002.png" alt="blog002"></p>
<p>目前显示的是hexo的默认主题，后面我们将对博客进行美化和优化。</p>
<p>以及更详细的hexo命令可以参考官方文档：<a href="https://hexo.io/zh-cn/docs/commands">https://hexo.io/zh-cn/docs/commands</a></p>
<p>现在一个本地的个人博客已经搭建成功了，接下来我们只要将这个博客静态页面代码托管到GitHub上，再使用GitHub Pages进行建站和部署到互联网上，这样后面只要输入网址就能访问到博客。不过GitHub是国外站点，国内访问时常打不开，所以现在<strong>需要解决GitHub打不开</strong>的问题，然后注册一个账户。</p>
<h2 id="优化访问GitHub"><a href="#优化访问GitHub" class="headerlink" title="优化访问GitHub"></a>优化访问GitHub</h2><p>优化GitHub访问的方法很多这里只展示我使用方法，不过我加速GitHub后，执行一些命令会执行不成功，停止加速再执行就会成功，后面我会在相应位置标记。</p>
<p>使用软件“Watt Toolkit”，原名“steam++”，是一个开源跨平台的多功能Steam工具箱，可以提供本地反代的功能，支持加速Steam、GitHub等平台。下载地址官网如下：</p>
<p><a href="https://steampp.net/">https://steampp.net/</a></p>
<p>使用也很简单，按照下面图片标注点击即可：</p>
<p><img src="/2026/01/15/hexo%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/blog003.png" alt="images003"></p>
<h2 id="GitHub项目文件托管"><a href="#GitHub项目文件托管" class="headerlink" title="GitHub项目文件托管"></a>GitHub项目文件托管</h2><p>GitHub的文件托管流程以我的理解如下：</p>
<h3 id="注册GitHub账号"><a href="#注册GitHub账号" class="headerlink" title="注册GitHub账号"></a>注册GitHub账号</h3><p>访问GitHub官网：<a href="https://github.com/">https://github.com/</a></p>
<p>注册流程按照要求来就行。</p>
<p>创建用户名时尽量短小，如果你没有域名的话，太长会很不方便的，当然这用户名是可以更改的。</p>
<h3 id="创建博客仓库"><a href="#创建博客仓库" class="headerlink" title="创建博客仓库"></a>创建博客仓库</h3><p>我们采用GitHub Pages的个人站点来部署，所以对仓库名称有严格要求：<strong>仓库名称.github.io</strong>，并且要将这个仓库处于<strong>公开</strong>状态，这个仓库名将是我们的博客地址。</p>
<h3 id="全局配置"><a href="#全局配置" class="headerlink" title="全局配置"></a>全局配置</h3><p>使用Git全局命令设置提交代码时的作者身份信息</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">git config --global user.name &quot;YourName&quot; # 用户名</span><br><span class="line">git config --global user.email &quot;your-email@example.com&quot; # 邮箱地址是你GitHub的注册邮箱</span><br></pre></td></tr></table></figure>

<p>参数解析：–global表示全局配置，对当前用户的所有仓库生效；不加这个参数的话，则只对当前仓库生效</p>
<p>有下面命令检查配置：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看所有配置</span></span><br><span class="line">git config --list</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">查看特定配置</span></span><br><span class="line">git config user.name</span><br><span class="line">git config user.email</span><br></pre></td></tr></table></figure>

<h3 id="生成SSH密钥对"><a href="#生成SSH密钥对" class="headerlink" title="生成SSH密钥对"></a>生成SSH密钥对</h3><p>我们用这密钥来代替HTTPS密码验证，这样更安全，而且一次配置，永久使用。</p>
<p>运行以下命令生成密钥对（选一条就行）：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ssh-keygen -t rsa -C &quot;your_email@example.com&quot;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">以下是更优的选择</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">使用更安全、更快的 Ed25519 算法</span></span><br><span class="line">ssh-keygen -t ed25519 -C &quot;your_email@example.com&quot;</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">或者指定更强的 RSA 密钥长度</span></span><br><span class="line">ssh-keygen -t rsa -b 4096 -C &quot;your_email@example.com&quot;</span><br></pre></td></tr></table></figure>

<p>执行这个命令过程直接三次回车默认设置就可以了，它会再 ~&#x2F;.ssh&#x2F; 目录生成两个文件：id_rsa和id_rsa.pub,其中id_rsa是私钥，需要保密，不能分享；id_rsa.pub是公钥，后面需要把它添加到GitHub，用来身份核对。</p>
<h3 id="查看公钥内容："><a href="#查看公钥内容：" class="headerlink" title="查看公钥内容："></a>查看公钥内容：</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">显示公钥内容，复制全部文本</span></span><br><span class="line">cat ~/.ssh/id_rsa.pub</span><br></pre></td></tr></table></figure>

<h3 id="将公钥添加到GitHub"><a href="#将公钥添加到GitHub" class="headerlink" title="将公钥添加到GitHub"></a>将公钥添加到GitHub</h3><ol>
<li>登录 GitHub → Settings → SSH and GPG keys</li>
<li>点击 “New SSH key”</li>
<li>粘贴 <code>id_rsa.pub</code> 的全部内容，保存</li>
</ol>
<h3 id="测试连接"><a href="#测试连接" class="headerlink" title="测试连接"></a>测试连接</h3><p>这里测试的时候可能会不成功，需要你将你的Watt Toolkit停止加速</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">测试是否配置成功</span></span><br><span class="line">ssh -T git@github.com</span><br><span class="line"><span class="meta prompt_"></span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">成功会显示：</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">Hi username! You<span class="string">&#x27;ve successfully authenticated...</span></span></span><br></pre></td></tr></table></figure>

<h3 id="Hexo核心部署插件"><a href="#Hexo核心部署插件" class="headerlink" title="Hexo核心部署插件"></a>Hexo核心部署插件</h3><p>我们不需要将所有的代码都上传到git仓库上，只需要将本地生成的静态博客网站文件上传。使用hexo静态博客框架的hexo-deployer-git这个部署插件可以让我们用一条命令将博客文章发布到网上。</p>
<p>接下来，我们来进行布置：</p>
<ol>
<li><p>使用以下命令安装hexo-deployer-git插件</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm install hexo-deployer-git --save</span><br></pre></td></tr></table></figure>
</li>
<li><p>配置Hexo主配置文件_config.yml，打开找到url和deploy部分并按以下格式修改：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">url: https://username.github.io # 博客地址</span><br></pre></td></tr></table></figure>



<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">deploy:</span><br><span class="line">  type: git</span><br><span class="line">  repo: git@github.com:你的用户名/你的仓库名.git # 推荐用 SSH 地址</span><br><span class="line"><span class="meta prompt_">  # </span><span class="language-bash">或使用 HTTPS 地址: https://github.com/你的用户名/你的仓库名.git,仓库名注意哦</span></span><br><span class="line">  branch: main # github默认分支是main</span><br><span class="line"><span class="meta prompt_">  # </span><span class="language-bash">branch 是关键: 这里设置的是静态文件将被推送到的分支</span></span><br></pre></td></tr></table></figure>
</li>
<li><p>执行部署</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">hexo clean;hexo generate;hexo deploy </span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">或简写:</span> </span><br><span class="line">hexo cl;hexo d -g</span><br></pre></td></tr></table></figure>

<p>将本地生成的静态博客网站文件上传到GitHub仓库中。</p>
</li>
</ol>
<p>托管部分结束了，后面进行建站，将静态网页部署到互联网就可以了。</p>
<h2 id="Github-Pages部署"><a href="#Github-Pages部署" class="headerlink" title="Github Pages部署"></a>Github Pages部署</h2><p>GitHub上免费快捷的静态网站托管服务，能部署两种类型站点：个人站点、项目站点</p>
<p>个人站点：每个用户只有1个，仓库名有严格要求（仓库名称.github.io）,访问地址是https:&#x2F;&#x2F;仓库名.github.io</p>
<p>项目站点：数量无限，仓库名没有限制，访问地址是<a href="https://github用户名.github.io/%E4%BB%93%E5%BA%93%E5%90%8D">https://github用户名.github.io/仓库名</a></p>
<p>关于网址长的问题自然有解决方法：配置自定义域名</p>
<p>部署流程：</p>
<p>进入仓库页面——&gt;Settings——&gt;Pages——&gt;选择部署方式为”Deploy from a branch”</p>
<ol>
<li><p>进入博客网站仓库设置界面</p>
</li>
<li><p>进入Pages服务页面</p>
</li>
<li><p>选择构建部署方式为：Deploy from a branch</p>
</li>
<li><p>Branch（分支）:分别选择main，  &#x2F;(root)，然后save保存，等待几分钟刷新即可</p>
</li>
<li><p>验证</p>
<p>访问 网址<code>https://username.github.io</code>查看效果</p>
</li>
</ol>
<p>这里之所以不选则GitHub Actions部署，是因为我们这个博客是在本地构建出静态文件，然后将这些静态文件上传到GitHub上的，actions服务的工作流脚本需要在博客根目录下执行，并且使用’hexo deploy’上传部署是执行的<strong>强制推送</strong>，会覆盖原来的仓库的文件，导致工作流脚本缺失。</p>
<h2 id="测试站点"><a href="#测试站点" class="headerlink" title="测试站点"></a>测试站点</h2><p>能够正常访问之后，使用<a href="https://www.boce.com/">拨测</a>检测一下域名解析与访问的速度<br><img src="/2026/01/15/hexo%E4%B8%AA%E4%BA%BA%E5%8D%9A%E5%AE%A2%E6%90%AD%E5%BB%BA/blog004.png" alt="images004"></p>
<h2 id="博客的简单使用"><a href="#博客的简单使用" class="headerlink" title="博客的简单使用"></a>博客的简单使用</h2><h3 id="写博客"><a href="#写博客" class="headerlink" title="写博客"></a>写博客</h3><p>使用以下命令：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">hexo new &quot;博客标题&quot; </span><br></pre></td></tr></table></figure>

<h3 id="上传博客"><a href="#上传博客" class="headerlink" title="上传博客"></a>上传博客</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">清除缓存文件 (db.json) 和已生成的静态文件 (public)。</span></span><br><span class="line">hexo clean       # 简写 hexo cl</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">生成静态文件。</span></span><br><span class="line">hexo generate    # 简写 hexo g</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">部署你的网站。</span></span><br><span class="line">hexo deploy      # 简写 hexo d</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">三条命令同时执行</span></span><br><span class="line">hexo cl;hexo g -d # powershell7+以下可用</span><br><span class="line">hexo cl &amp;&amp; g -d  # 仅限powershell7+的版本</span><br></pre></td></tr></table></figure>

<p>访问博客验证是否上传成功，可能存在延迟，需要等一会</p>
<h3 id="删除博客"><a href="#删除博客" class="headerlink" title="删除博客"></a>删除博客</h3><p>进入存放博客文章的目录source_posts中，将你要删除的文章删除，然后执行以下命令：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">hexo cl;hexo g -d</span><br></pre></td></tr></table></figure>

<p>访问博客地址验证</p>
]]></content>
      <categories>
        <category>个人博客</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
</search>
